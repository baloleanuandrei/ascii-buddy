# asciibuddy playground

Next.js app: pick a pack, tweak the brand, preview the rendered components in your browser, download a `brand.json`.

```bash
pnpm --filter @asciibuddy/web install
pnpm --filter @asciibuddy/web dev
```

Then run `npx asciibuddy init --config brand.json` with the downloaded file.
