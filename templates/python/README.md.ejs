# <%= spec.name %> CLI UI (Python)

Generated by asciibuddy. Themed CLI components for <%= spec.name %>.

```python
from <%= pyModuleName %> import banner, success, error, warning, info, section, spinner

banner()
section("Configuration")
with spinner("Deploying..."):
    do_work()
success("Deployed")
```

Run the preview:

```bash
python -m <%= pyModuleName %>.preview
```
