{
  "name": "<%= spec.packageName %>",
  "version": "0.1.0",
  "description": "<%= spec.name %> CLI UI components — generated by asciibuddy.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "preview": "tsx src/preview.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0"<% if (has("table")) { %>,
    "cli-table3": "^0.6.5"<% } %><% if (has("spinner") || has("progress")) { %>,
    "ora": "^8.0.1"<% } %><% if (has("prompts")) { %>,
    "@clack/prompts": "^0.7.0"<% } %><% if (has("progress")) { %>,
    "cli-progress": "^3.12.0",
    "@types/cli-progress": "^3.11.5"<% } %><% if (has("box")) { %>,
    "boxen": "^7.1.1",
    "diff": "^5.2.0",
    "@types/diff": "^5.2.1"<% } %><% if (has("code")) { %>,
    "cli-highlight": "^2.1.11"<% } %><% if (has("markdown")) { %>,
    "marked": "^12.0.0",
    "marked-terminal": "^7.1.0",
    "@types/marked-terminal": "^6.1.1"<% } %><% if (has("gradient")) { %>,
    "gradient-string": "^2.0.2",
    "@types/gradient-string": "^1.1.6"<% } %><% if (has("qr")) { %>,
    "qrcode-terminal": "^0.12.0",
    "@types/qrcode-terminal": "^0.12.2"<% } %><% if (renderer === "ink") { %>,
    "ink": "^5.0.1",
    "react": "^18.3.1"<% } %>
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.12.0"<% if (renderer === "ink") { %>,
    "@types/react": "^18.3.3"<% } %>
  }
}
