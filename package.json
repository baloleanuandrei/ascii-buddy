{
  "name": "asciibuddy-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @asciibuddy/cli dev",
    "init": "pnpm --filter @asciibuddy/cli start init",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.12.0",
    "ejs": "^3.1.10",
    "@types/ejs": "^3.1.5",
    "vitest": "^2.0.0",
    "@asciibuddy/core": "workspace:*",
    "@asciibuddy/pack-minimal-mono": "workspace:*",
    "@asciibuddy/pack-retro-terminal": "workspace:*",
    "@asciibuddy/pack-candy": "workspace:*"
  }
}
